class LTXVideoProcessor:
    """Simple placeholder processor for the `ltx` style."""

    def process(self, data):
        """Process the provided data and return a message for testing."""
        return f"ltx processed {data}"
